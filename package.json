{
  "name": "gcloud-identity-token-generator",
  "version": "1.0.0",
  "description": "A Node.js server intended to only be run locally, to generate Google Cloud identity tokens using a bash script.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "lint": "eslint .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
