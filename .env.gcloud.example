# .env.gcloud.example

# IMPORTANT SECURITY NOTE:
# This file contains template environment variables for Google Cloud configurations.
# NEVER commit your actual credentials or sensitive data to version control systems.
# Treat your service account JSON and any sensitive variables with high confidentiality.
# Ensure that the .env.gcloud file, which will contain your actual credentials, is added to .gitignore or equivalent in your version control system.

# Service Account JSON (replace with your actual service account JSON, ensure it's a single line string)
# This JSON is used for authenticating with Google Cloud services.
SERVICE_ACCOUNT_JSON='{"type": "service_account", "project_id": "your_project", "private_key_id": "your_private_key_id", ...}'

# Audience URL for the gcloud identity token
# This is the URL of the Google Cloud service (e.g., Cloud Run) that the identity token will be used to access.
GCLOUD_IDENTITY_TOKEN_AUDIENCE='https://your-cloud-run-service-url.a.run.app'
